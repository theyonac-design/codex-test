# codex-test

シンプルな `index.html` を表示するためのサンプルです。

## 使い方

1. リポジトリをクローンします。
2. `index.html` をブラウザで開きます。

例:

```bash
xdg-open index.html
```

または Python の簡易サーバーを使って表示できます。

```bash
python -m http.server 8000
```

ブラウザで `http://localhost:8000` にアクセスすると、`Hello Codex` が表示されます。
